import { UniqueEmailValidatiorDirective } from './unique-email-validatior.directive';

describe('UniqueEmailValidatiorDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueEmailValidatiorDirective();
    expect(directive).toBeTruthy();
  });
});
